# Database connection (required)
# Example for a local Postgres instance; update with real credentials.
DATABASE_URL="postgresql://user:password@localhost:5432/db"

# NextAuth (required)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="e3ff5f077839c1331b1d893a728246685cb7dba9e3a77bffe7d52eaccf660988"

# Email configuration (optional - for notifications)
# Used for:
# - Habilitation expiration alerts (sent to user email)
# - Broken tool alerts (sent to all admin emails)
# - Problem notifications (sent to ADMIN_EMAIL)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-gmail-app-password"
ADMIN_EMAIL="admin@yourcompany.com" 